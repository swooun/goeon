#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

/*int main(void) {
	
	int flag = 1;
	while(flag != 0)
	{
		int y;
		y = 3;
		flag = 0;
	}
	y=4;
}*/

int main(void) {
	
	int flag = 1;
	int y; /* y가 정의되지 않아 윗 줄에 int y를 추가함*/ 
	while(flag != 0)
	{
		int y;
		y = 3;
		flag = 0;
	}
	y=4;
}

